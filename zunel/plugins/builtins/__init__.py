"""Reference plugins shipped in-tree for documentation/testing.

These directories are NOT auto-loaded by :class:`PluginManager`. They
live under ``zunel.plugins.builtins`` so curious users can read a real
``plugin.yaml`` + ``plugin.py`` pair without having to clone an
external repo. To use one, copy the directory into
``<ZUNEL_HOME>/plugins/<name>/`` and edit it.
"""
